name: photo_shader
description: A set of shader tools for photos.
homepage: https://matthewrice.dev

version: 1.0.0+1

environment:
  sdk: ^3.3.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.3
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  shaders:
    - assets/shaders/photo_shader.frag
